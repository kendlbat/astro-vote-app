---
const returnUrl = Astro.url.searchParams.get("r") || "/";
---

<script data-returnurl={returnUrl}>
    const { signIn } = await import("auth-astro/client");

    signIn("azure-ad", {
        callbackUrl: this.dataset.returnurl,
    });
</script>
